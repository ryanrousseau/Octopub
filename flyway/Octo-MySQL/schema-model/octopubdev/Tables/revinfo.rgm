table "octopubdev"."revinfo" {
  columns = (
    column "REV" {
      type = int
      null = NOT NULL
    }
    column "REVTSTMP" {
      type = bigint
    }
    column "foo" {
      type = varchar(100)
    }
  )
  primaryKey = {
    columns = [
      REV
    ]
    name = "PRIMARY"
    objectType = primaryKey
  }
}

